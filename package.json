{
  "name": "specboard",
  "version": "1.0.0",
  "description": "Dashboard to monitor OpenSpec progress across workspaces",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts"
  }
}
